# Nextcloud image tag (see hub.docker.com/_/nextcloud)
# Pinned rather than `stable`: `stable` is now Nextcloud 34, and the app
# declares max-version="33" in appinfo/info.xml, so it refuses to enable there.
NEXTCLOUD_IMAGE_TAG=33

# Host port for Nextcloud
NEXTCLOUD_PORT=8580

# Admin credentials (first-run auto-setup)
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=admin

# MariaDB
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud
MYSQL_PASSWORD=nextcloud

# Xdebug: off | develop | debug | develop,debug | coverage
XDEBUG_MODE=develop,debug
